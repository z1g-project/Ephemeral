import { cn } from "@/lib/utils";
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
export default function CloakSettings(props: cardProps) {
    return (
        <>
        <Card className={cn("h-96 w-96",props.className)}>
            
        </Card>
        </>
    )
}